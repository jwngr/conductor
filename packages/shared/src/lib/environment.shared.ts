import {Environment} from '@shared/types/environment.types';

export const ALL_CLIENT_ENVIRONMENTS = [Environment.PWA, Environment.Extension];
export const ALL_SERVER_ENVIRONMENTS = [Environment.Server];
export const ALL_ENVIRONMENTS = [Environment.PWA, Environment.Extension, Environment.Server];
