{
  "include": ["src"],
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Required since this is a TypeScript referenced project.
    "composite": true,

    // Build output.
    "rootDir": "./src",
    "outDir": "./dist"
  }
}
