{
  "name": "@conductor/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@conductor/shared": "1.0.0",
    "jsdom": "^25.0.1"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.9.0"
  },
  "scripts": {
    "validate": "npm run validate:lint && npm run validate:types && npm run validate:format",
    "validate:lint": "eslint src --report-unused-disable-directives --max-warnings 0",
    "validate:types": "tsc --noEmit",
    "validate:format": "prettier --check 'src/**/*.{js,ts,json}'",
    "format": "prettier --write 'src/**/*.{js,ts,json}'",
    "ci": "rm -rf node_modules/ && npm install",
    "build": "# scripts are run via tsx, not compiled, so there's nothing to build",
    "clean": "# scripts are run via tsx, not compiled, so there's nothing to clean"
  }
}
